sudo rm -rf ~/idemiafoot-back
if [ ! -d ~/idemiafoot-back ]; then mkdir -p ~/idemiafoot-back; fi
docker-compose.yml ubuntu@129.151.232.234:~/idemiafoot-back
docker ps -a -q | xargs -r docker stop --force
docker ps -a -q | xargs -r docker rm --force
docker system prune --all --force
pid=$(sudo lsof -t -i:3306)
if [ -n "$pid" ]; then
  sudo kill $pid
fi